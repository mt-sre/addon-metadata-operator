
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: addontemplates.addons.managed.openshift.io
spec:
  group: addons.managed.openshift.io
  names:
    kind: AddonTemplate
    listKind: AddonTemplateList
    plural: addontemplates
    singular: addontemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AddonTemplate is the Schema for the addontemplates API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: 'AddonTemplateSpec defines the desired state of AddonTemplate
              View markers: $ controller-gen -www crd TODO add missing fields from
              schema, simplified type for POC'
            properties:
              description:
                type: string
              enabled:
                type: boolean
              icon:
                description: TODO base64
                type: string
              id:
                pattern: ^[A-Za-z0-9][A-Za-z0-9-]{0,30}[A-Za-z0-9]$
                type: string
              label:
                pattern: ^api\.openshift\.com/addon-[0-9a-z][0-9a-z-]{0,30}[0-9a-z]$
                type: string
              link:
                pattern: ^http[s]?://(?:[a-zA-Z]|[0-9]|[$-_@.&+]|[!*\(\),]|(?:%[0-9a-fA-F][0-9a-fA-F]))+$
                type: string
              name:
                pattern: ^[0-9A-Z][A-Za-z0-9-_ ()]+$
                type: string
            required:
            - description
            - enabled
            - icon
            - id
            - label
            - name
            type: object
          status:
            description: AddonTemplateStatus defines the observed state of AddonTemplate
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
